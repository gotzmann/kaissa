import chess
import random

# TODO 

def best_move(moves: Iterator[chess.Move]):
    """
    Evaluate move for negamax algorithm.    
    """

    moves = list(board.legal_moves)
    #move = random.choice(moves)
    return random.randint(0, 10000)